# grokking-transformer

A decoder-only transformer implemented from scratch (attention included) and
trained on modular addition to reproduce **grokking**: test accuracy jumping
from chance to ~100% thousands of steps *after* the training set is fully
memorized. Full README with the study's results coming as the experiments land.
